# Normalize line endings across the repository

# Default behavior: let Git detect text files and normalize line endings in the repo
* text=auto

# Always use LF for shell scripts and specs to avoid /usr/bin/env: 'bash\r' errors on Linux
*.sh text eol=lf
*.bash text eol=lf
scripts/** text eol=lf
*.spec text eol=lf

# YAML and CI config should also be LF to avoid tool parsing issues
*.yml text eol=lf
*.yaml text eol=lf

# Source files: prefer LF consistently
*.py text eol=lf

# Keep Windows batch files with default platform endings
*.bat text
